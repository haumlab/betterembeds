# Port to run the server on (default: 3000)
PORT=3000

# Optional: Facebook App access token for better Facebook embed support
# Get one at https://developers.facebook.com/
# Without this, Facebook falls back to scraping public OG tags
FACEBOOK_ACCESS_TOKEN=
